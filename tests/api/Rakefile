require 'pry'
require_relative 'lib/scenarios_list'
require_relative 'lib/step_runner'

task :default do
  list = ScenariosList.new('./scenarios')
  scenario = list.all.first

  scenario.steps.each do |step|
    StepRunner.new(step).call
  end
end
